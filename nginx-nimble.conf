    # NimblePhysics Visualizer (HTTP)
}

# HTTPS Server
server {
    listen 443 ssl;
    server_name _;

    ssl_certificate /etc/nginx/ssl/ca/server.crt;
    ssl_certificate_key /etc/nginx/ssl/ca/server.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /var/www/physioevalab;
    index index.html index.php;

    # Gait Benchmark Dashboard (Quarto+Shiny)
    location /gait/ {
        rewrite ^/gait/(.*)$ /$1 break;
        proxy_pass http://127.0.0.1:3841;
        proxy_http_version 1.1;
--
    # NimblePhysics Visualizer (HTTP)
    location /nimble {
        return 301 /nimble/;
